import { PageSkeleton } from '@/components/ui/skeleton';

export default function AppSettingsAuditorAccessNewLoading() {
  return <PageSkeleton title="New auditor grant" cards={1} tableRows={0} />;
}
