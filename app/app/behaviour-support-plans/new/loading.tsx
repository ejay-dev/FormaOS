import { PageSkeleton } from '@/components/ui/skeleton';

export default function AppBehaviourSupportPlansNewLoading() {
  return <PageSkeleton title="New behaviour support plan" cards={2} tableRows={0} />;
}
