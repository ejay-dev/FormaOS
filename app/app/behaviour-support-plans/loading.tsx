import { PageSkeleton } from '@/components/ui/skeleton';

export default function AppBehaviourSupportPlansLoading() {
  return <PageSkeleton title="Behaviour support plans" cards={0} tableRows={5} />;
}
